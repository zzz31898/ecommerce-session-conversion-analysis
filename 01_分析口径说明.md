# 电商网站会话行为与购买转化分析：分析口径说明

## 1. 文档信息

- 文档状态：初稿，待项目负责人确认
- 对应阶段：第一步——明确分析范围与指标口径
- 当前数据文件：`online_shoppers_intention.csv`
- 项目名称：电商网站会话行为与购买转化分析

本阶段只统一分析对象、业务问题、指标定义和结论边界，不进行数据清洗、统计检验、预测建模或 Power BI 看板制作。

## 2. 项目背景与目标用户

### 2.1 项目目标

识别与网站会话购买转化显著相关的行为特征，并寻找值得优先优化的会话类型和流量来源，为产品、增长和网站运营提供决策依据。

### 2.2 目标用户

- 电商产品经理：定位影响购买转化的页面行为和重点优化场景。
- 用户增长负责人：判断不同流量来源的规模与转化质量，寻找增长机会。
- 网站运营负责人：监控整体、月份、周末及访客类型的转化表现。
- 产品数据分析师：完成口径管理、统计验证、建模和结果解释。

## 3. 数据概况

### 3.1 已核对的数据事实

- 数据行数：12,330 行。
- 原始字段数：18 个。
- 数据粒度：一行代表一个网站访问会话。
- 购买结果字段：`Revenue`，当前值为 `TRUE` 或 `FALSE`。
- 访客类型：`New_Visitor`、`Returning_Visitor`、`Other`。
- 周末标记：`Weekend`，当前值为 `TRUE` 或 `FALSE`。
- 月份类别：`Feb`、`Mar`、`May`、`June`、`Jul`、`Aug`、`Sep`、`Oct`、`Nov`、`Dec`。
- 数据中没有真实用户 ID、会话时间戳、订单编号、订单金额、商品明细和逐条点击事件。

### 3.2 分析单位

本项目的基本分析单位是“会话”，不是可跨时间追踪的“用户”。

- 会话数量等于满足当前筛选条件的数据行数。
- 购买会话是 `Revenue = TRUE` 的数据行。
- 未购买会话是 `Revenue = FALSE` 的数据行。
- 后续可以新增从 1 开始、每行唯一的 `SessionID`，但该编号只用于识别数据行，不能用于连接同一访客的多次访问。

虽然原始数据提供了 `VisitorType`，但它只能作为数据源给出的会话标签使用。没有用户 ID，因此无法独立验证某位访客是否真的再次访问，也不能进行跨会话追踪。

## 4. 核心业务问题

### 4.1 网站整体表现

1. 网站整体会话购买转化率是多少？
2. 购买会话与未购买会话在数量和行为上有何差异？
3. 各月份的会话量和转化率是否存在差异？
4. 周末与工作日的会话量和转化率是否不同？

### 4.2 访客与流量质量

1. 新访客、回访访客及其他访客会话的转化表现有何差异？
2. 哪些 `TrafficType` 带来的会话量最大？
3. 哪些 `TrafficType` 的购买转化率较高？
4. 哪些来源属于“会话量较大但转化率较低”的优先优化对象？
5. 不同操作系统、浏览器和地区的会话表现是否存在明显差异？

### 4.3 页面行为与增长机会

1. 产品相关页面浏览量与购买转化率有什么关系？
2. 产品相关页面停留时间与购买转化率有什么关系？
3. 信息类和管理类页面访问行为与购买结果有什么关系？
4. 高跳出率和高退出率会话具有什么特征？
5. 哪些未购买会话在可观测行为上更接近购买会话？
6. 产品和增长团队应优先优化哪些会话类型？

## 5. 核心字段与业务含义

| 字段 | 本项目中的含义 | 使用注意事项 |
| --- | --- | --- |
| `Administrative` | 会话中管理类页面访问数量 | 不能还原具体页面或访问顺序 |
| `Administrative_Duration` | 管理类页面停留时间 | 单位需在数据字典或来源说明中再次确认 |
| `Informational` | 会话中信息类页面访问数量 | 不能等同于独立页面数 |
| `Informational_Duration` | 信息类页面停留时间 | 较长停留不一定代表更好体验 |
| `ProductRelated` | 会话中产品相关页面浏览数量 | 用于衡量产品浏览深度，但不是完整漏斗阶段 |
| `ProductRelated_Duration` | 产品相关页面停留时间 | 需与浏览数量结合解释 |
| `BounceRates` | 与该会话页面相关的平均跳出率 | 不能仅凭高跳出率认定页面存在故障 |
| `ExitRates` | 与该会话页面相关的平均退出率 | 退出不必然代表负面体验 |
| `PageValues` | 完成电商交易前访问页面的平均价值 | 可能包含临近购买时点的信息，预测时需单独处理 |
| `SpecialDay` | 访问日期接近特殊节日的程度 | 不是节日名称，也不一定表示当天就是节日 |
| `Month` | 会话所在月份类别 | 没有日期、年份，且缺少 1 月和 4 月 |
| `OperatingSystems` | 操作系统类别编码 | 未获得映射表前保留编码，不擅自命名 |
| `Browser` | 浏览器类别编码 | 未获得映射表前保留编码，不擅自命名 |
| `Region` | 地区类别编码 | 未获得映射表前保留编码，不擅自解释地理名称 |
| `TrafficType` | 流量来源类别编码 | 未获得映射表前不能解释为搜索、社交或广告等具体渠道 |
| `VisitorType` | 数据源标记的访客类型 | 只用于会话分组，不用于用户级追踪 |
| `Weekend` | 会话是否发生在周末 | `TRUE` 为周末，`FALSE` 为工作日 |
| `Revenue` | 会话是否产生购买 | 只表示是否购买，不包含订单数和收入金额 |

## 6. 派生字段定义

本节先规定后续字段口径，实际生成字段属于数据处理阶段。

### 6.1 会话编号

| 字段 | 定义 |
| --- | --- |
| `SessionID` | 从 1 开始的唯一行索引 |

`SessionID` 仅用于唯一标识会话记录，不代表真实用户 ID。

### 6.2 购买标记

保留原始 `Revenue` 字段，并新增数值字段，避免破坏原始信息。

| 字段 | 转换规则 |
| --- | --- |
| `RevenueFlag` | `Revenue = TRUE` 转换为 1；`Revenue = FALSE` 转换为 0 |

### 6.3 产品页浏览深度

| 分组 | `ProductRelated` 范围 |
| --- | ---: |
| `0至5` | 0 至 5 |
| `6至10` | 6 至 10 |
| `11至20` | 11 至 20 |
| `21至49` | 21 至 49 |
| `50及以上` | 大于或等于 50 |

使用中文范围标签，避免 Excel 将 `6-10`、`11-20` 等文本自动识别为日期。所有分组互斥且完整，数值 50 归入 `50及以上`。

### 6.4 产品页停留时长分组

`ProductRelated_Duration` 按有效样本分位数划分为低、中、高三组：

- 低：小于或等于第 33.33 百分位。
- 中：大于第 33.33 百分位且小于或等于第 66.67 百分位。
- 高：大于第 66.67 百分位。

正式生成字段前需要检查零值比例和分位点是否重复。如果大量零值导致分位点相同，应先将“零停留”单独成组，再对非零值计算分位数，不能强行分成三个失真的组。

### 6.5 高跳出和高退出会话

| 字段 | 定义 |
| --- | --- |
| `HighBounceSession` | `BounceRates` 大于全体有效会话的第 75 百分位 |
| `HighExitSession` | `ExitRates` 大于全体有效会话的第 75 百分位 |

默认使用严格大于第 75 百分位的规则。若临界值重复造成实际占比明显偏离 25%，应在结果中报告真实占比，不人为调整记录。

### 6.6 访客类型标准化

标准化结果保持三类：

- `Returning_Visitor`
- `New_Visitor`
- `Other`

空值、拼写异常或无法识别的值是否归入 `Other`，需要在数据质量检查后决定并记录。

### 6.7 月份排序

新增 `MonthOrder`，按自然月份排序：

| `Month` | `MonthOrder` |
| --- | ---: |
| `Feb` | 2 |
| `Mar` | 3 |
| `May` | 5 |
| `June` | 6 |
| `Jul` | 7 |
| `Aug` | 8 |
| `Sep` | 9 |
| `Oct` | 10 |
| `Nov` | 11 |
| `Dec` | 12 |

数据未包含 1 月和 4 月，不能补造这两个月的会话记录。

## 7. 核心指标口径

| 指标 | 定义 | 计算说明 |
| --- | --- | --- |
| 会话数量 | 当前筛选范围内的记录数 | `COUNTROWS` |
| 购买会话数 | `Revenue = TRUE` 的会话数 | 也等于 `RevenueFlag` 求和 |
| 未购买会话数 | `Revenue = FALSE` 的会话数 | 会话数量减购买会话数 |
| 会话购买转化率 | 购买会话数除以会话数量 | 分母为 0 时返回空值 |
| 新访客会话占比 | `New_Visitor` 会话数除以会话数量 | 不是新增用户占比 |
| 回访访客会话转化率 | 回访访客购买会话数除以回访访客会话数 | 不是复购率 |
| 平均产品页浏览量 | `ProductRelated` 的平均值 | 建议同时报告中位数和分布 |
| 周末会话转化率 | 周末购买会话数除以周末会话数 | 与工作日使用相同定义 |
| 工作日会话转化率 | 工作日购买会话数除以工作日会话数 | 与周末使用相同定义 |

所有分类转化率图表都应同时展示或允许查看对应会话数量，避免将小样本类别的高转化率误判为高质量来源。

## 8. 分析范围

### 8.1 可以开展的分析

- 会话购买转化分析。
- 新访客、回访访客及其他访客会话比较。
- 流量来源类别的会话量和转化质量分析。
- 页面访问数量、停留时间与购买结果的关联分析。
- 跳出率、退出率与购买结果的关联分析。
- 周末和工作日差异分析。
- 样本覆盖月份之间的表现差异分析。
- 高潜未购买会话识别。
- 基于会话特征的购买倾向预测。

### 8.2 不可以声称开展的分析

- 用户留存分析。
- Cohort 分析。
- 复购分析。
- 用户生命周期价值分析。
- 用户跨会话路径分析。
- 完整的浏览—加购—结算—支付漏斗分析。
- 客单价、销售额或利润分析。
- 渠道投放 ROI 分析。
- 商品或品类表现分析。
- 页面行为对购买的因果影响分析。

## 9. 时间与季节性结论边界

数据只有月份类别，没有日期和年份，而且只包含 10 个月份。因此：

- 可以比较样本中不同月份的会话量和转化率。
- 可以描述“样本期内的月份差异”。
- 不能验证同比、环比或跨年度趋势。
- 不能仅凭当前数据认定存在稳定的季节性规律。
- 月份差异可能同时受到流量结构、访客类型和特殊节日等因素影响。

后续报告建议使用“月份差异”而不是直接使用“季节性”，除非获得跨年且月份完整的数据。

## 10. 流量来源结论边界

`TrafficType` 当前是类别编码，尚无业务名称、获客成本或投放费用。因此：

- 可以比较各编号的会话量、购买会话数和转化率。
- 可以识别会话量较大但转化率较低的来源编号。
- 不能擅自把编号解释为自然搜索、付费广告、社交媒体或直接访问。
- 不能评价渠道盈利能力或 ROI。
- 业务建议应写成“优先核查/优化 TrafficType X”，直到获得渠道映射表。

## 11. `PageValues` 的使用规则

`PageValues` 表示用户完成电商交易前访问页面的平均价值，可能与购买结果存在非常强的关联，也可能只在会话后期才可用。

因此后续预测应建立两个口径不同的模型：

1. 会话末期模型：包含 `PageValues`，用于判断会话接近结束时的购买可能性。
2. 早期购买倾向模型：排除 `PageValues`，用于模拟更早阶段可采取干预的场景。

两个模型的结果必须分开解释。不能因为包含 `PageValues` 的模型分数较高，就直接认定它更适合实时早期干预。

## 12. 分析与表达原则

### 12.1 结果展示原则

- 转化率必须配合会话量或样本量展示。
- 平均值应尽量配合中位数、分位数或分布展示。
- 组间差异除显著性检验外，还应报告效应量。
- 未购买会话约占多数时，模型不能只报告准确率。
- 高潜会话的定义应基于多项行为特征或模型结果，不能只用单一字段下结论。

### 12.2 用词规范

优先使用：

- “会话购买转化率”
- “回访访客会话”
- “高潜未购买会话”
- “与购买显著相关”
- “存在统计差异”
- “预测贡献较高”

避免使用：

- “用户复购率”
- “用户留存率”
- “完整购买漏斗”
- “该行为导致购买”
- “该渠道一定更优”
- “长期季节性规律”

## 13. 后续待确认事项

以下事项不阻塞第一步，但在数据处理或分析前应完成确认：

1. 各停留时间字段的单位是否为秒。
2. `TrafficType`、`OperatingSystems`、`Browser` 和 `Region` 是否存在官方业务映射表。
3. 业务方更重视识别潜在购买会话的召回率，还是减少错误触达的精确率。
4. 高潜会话用于实时干预、运营复盘，还是渠道优化，不同场景会影响特征可用时间点。
5. Power BI 中是否保留英文原始字段，同时增加中文显示名称。
6. 后续是否能补充日期、年份、渠道名称、渠道成本或订单金额数据。

## 14. 第一阶段验收标准

完成第一阶段后，应满足以下条件：

- 项目名称准确体现会话级分析边界。
- 明确一行代表一个会话，`SessionID` 不被误用为用户 ID。
- `Revenue`、购买会话数和会话购买转化率的定义唯一且一致。
- 每个核心业务问题都能由现有字段支持，或已清楚标注限制。
- 明确不能开展留存、复购、LTV、跨会话路径和完整漏斗分析。
- 月份、渠道编码和 `PageValues` 的解释限制已记录。
- 后续 Python、Power BI 和报告使用同一套指标名称与计算口径。
- 任何结论都能说明统计对象、计算方式、业务含义以及不能支持的推论。
