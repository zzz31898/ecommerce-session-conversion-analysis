# 购买倾向模型评估补充报告

## 1. 本报告修正的模型定位

- 排除 `PageValues` 的模型使用的仍是整段会话聚合行为，因此它是“排除PageValues的会话行为模型”，不是实时早期干预模型。
- 包含 `PageValues` 的模型是回顾性性能基准。该字段与交易归因关系密切，在真实生成时点未被验证前，不用于部署评分。
- 历史会话评分统一使用训练集折外分数或测试集留出分数，不使用模型对自身训练记录产生的分数。

## 2. 优胜模型测试集诊断

| 场景 | 模型 | PR-AUC | ROC-AUC | Brier Score | Precision | Recall | F1 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 排除PageValues的会话行为模型 | 随机森林 | 0.358 | 0.766 | 0.150 | 30.17% | 59.16% | 39.96% |
| PageValues回顾性基准模型 | 随机森林 | 0.720 | 0.922 | 0.089 | 62.11% | 72.51% | 66.91% |

Brier Score和校准分箱用于诊断分数与实际转化率的对应程度；当前输出字段统一称为“倾向分数”，不将其直接表述为经过校准的购买概率。

## 3. 优胜模型测试集置信区间

| 场景 | 指标 | 95%下限 | 95%上限 | Bootstrap次数 |
| --- | --- | --- | --- | --- |
| 排除PageValues的会话行为模型 | Accuracy | 0.708 | 0.742 | 1000 |
| 排除PageValues的会话行为模型 | Precision | 0.279 | 0.325 | 1000 |
| 排除PageValues的会话行为模型 | Recall | 0.542 | 0.641 | 1000 |
| 排除PageValues的会话行为模型 | F1 | 0.369 | 0.431 | 1000 |
| 排除PageValues的会话行为模型 | ROCAUC | 0.743 | 0.788 | 1000 |
| 排除PageValues的会话行为模型 | PRAUC | 0.323 | 0.406 | 1000 |
| 排除PageValues的会话行为模型 | BrierScore | 0.144 | 0.155 | 1000 |
| PageValues回顾性基准模型 | Accuracy | 0.877 | 0.901 | 1000 |
| PageValues回顾性基准模型 | Precision | 0.586 | 0.659 | 1000 |
| PageValues回顾性基准模型 | Recall | 0.678 | 0.767 | 1000 |
| PageValues回顾性基准模型 | F1 | 0.635 | 0.702 | 1000 |
| PageValues回顾性基准模型 | ROCAUC | 0.908 | 0.934 | 1000 |
| PageValues回顾性基准模型 | PRAUC | 0.681 | 0.758 | 1000 |
| PageValues回顾性基准模型 | BrierScore | 0.083 | 0.094 | 1000 |

## 4. 排除PageValues模型的测试集Lift与触达档位

测试集最高分10%会话的实际转化率为 42.28%，测试集整体转化率的Lift为 2.73 倍。该结果来自未参与训练的测试记录。

| 触达Top比例 | 会话数 | 最低分数 | 捕获购买 | Precision | Recall | Lift |
| --- | --- | --- | --- | --- | --- | --- |
| 5.00% | 124 | 0.678 | 63 | 50.81% | 16.49% | 3.28 |
| 10.00% | 247 | 0.597 | 105 | 42.51% | 27.49% | 2.74 |
| 20.00% | 494 | 0.507 | 172 | 34.82% | 45.03% | 2.25 |
| 30.00% | 740 | 0.441 | 223 | 30.14% | 58.38% | 1.95 |

## 5. 特征可用性边界

| 特征组 | 可用时点 | 可用于实时早期干预 | 处理决定 |
| --- | --- | --- | --- |
| 会话入口或稳定属性 | 需由埋点/会话创建时确认 | 待确认 | 当前仅可作为会话级分析特征，不能假定实时可用。 |
| 整段会话聚合行为 | 会话进行中或结束后才逐步/完整形成 | 否 | 当前数据没有观察窗口和事件时间戳，因此不能称为早期实时特征。 |
| PageValues | 与交易归因关系密切，实际生成时点未验证 | 否 | 仅作为回顾性基准；在获得字段生成时点证据前不进入部署模型。 |

## 6. 历史高潜会话评分

未购买会话中，使用排除PageValues模型的历史样本外分数选出的Top 10%共有 1,043 条。评分来源字段区分 `train_oof` 与 `test_holdout`，确保每条历史会话的标签没有被用于拟合生成该条分数的模型。

该群体仍是回顾性分析对象，不能用其自身的已知未购买结果验证Lift；运营Lift应始终以测试集或未来新会话验证。

## 7. Power BI和SQL使用规则

1. 历史分析仅连接 `out_of_sample_session_scores.csv`，使用 `BehaviorExclPageValuesOutOfSampleScore` 和 `HistoricalHighPotentialTop10Percent`。
2. 不在Power BI中把分数显示为“购买概率”；使用“购买倾向分数”或“风险排序”。
3. 未来新会话由 `champion__early_propensity.joblib` 评分；该模型仍要求输入字段在实际评分时已可获得。
4. `PageValuesBenchmarkOutOfSampleScore` 仅作回顾性对照，不进入业务触达规则。

## 8. 输出文件

- `out_of_sample_session_scores.csv`
- `calibration_bins.csv`
- `test_decile_lift.csv`
- `operating_points.csv`
- `bootstrap_confidence_intervals.csv`
- `feature_availability.csv`
